#include "headers.h"

#include "it.h"

/* --------------------------------------------------------------------- */

char dir_modules[PATH_MAX + 1] = "/home/chisel/src/it/test";
char dir_samples[PATH_MAX + 1] = "/home/chisel/media/samples";
char dir_instruments[PATH_MAX + 1] = "/home/chisel/media/samples";

/* --------------------------------------------------------------------- */

/* TODO: actually load (and save) the settings... */
